story_id: e01s01
title: Fix normalizeMonitorOutput to correctly extract and flatten sample metrics
status: failing
bcps: 3
risk: P0
context: infra
delta: MODIFIED
tasks:
  - id: t01
    description: Add debug logging to normalizeMonitorOutput to trace input/output
    verify: pnpm dev && curl -s http://localhost:5678/api/debug-monitor | grep -E "normalizeMonitorOutput|normalized"
    risk: P3
    status: passing
    allure:
      severity: minor
      categories:
        - "Monitoring"
        - "Debug"
  - id: t02
    description: Verify normalizeMonitorOutput correctly extracts metrics from valid sample data
    verify: pnpm test lib/rpc/procedures/monitor.test.ts
    risk: P0
    status: passing
    allure:
      severity: critical
      categories:
        - "Monitoring"
        - "Fix"
  - id: t03
    description: Verify metrics display correctly in dashboard
    verify: pnpm dev && curl -s http://localhost:5678/dashboard | grep -E "CPU|GPU|Memory|Power" | head -4
    risk: P1
    status: failing
    allure:
      severity: high
      categories:
        - "Monitoring"
        - "UI"
