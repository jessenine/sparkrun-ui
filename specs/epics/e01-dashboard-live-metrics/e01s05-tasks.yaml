# e01s05-tasks.yaml

tasks:
  - title: Add process metrics RPC endpoint
    description: Add a new RPC endpoint `/rpc/monitor/processes` that returns per-process utilization data from `ps aux`
    deliverables:
      - `lib/rpc/procedures/processes.ts` - Implement normalizeProcessList function
      - `lib/rpc/procedures/monitor.ts` - Add `processes` handler
      - `lib/rpc/router.ts` - Register new route
      - Tests for the new endpoint
    status: passing
    estimated_hours: 3

  - title: Extend sparkrun cluster monitor output (if needed)
    description: Determine if sparkrun can provide per-process data, or if we need to run `ps aux` directly
    deliverables:
      - Decision document in `specs/tech-architecture/`
      - Any necessary backend extensions
    status: passing
    estimated_hours: 2

  - title: Build ProcessList component
    description: Create a new React component to display top 5 processes in a dashboard card
    deliverables:
      - `app/components/dashboard/ProcessList.tsx`
      - Storybook story for the component
      - Unit tests
    status: passing
    estimated_hours: 4

  - title: Update ProcessList component with missing features
    description: Add sort toggle (CPU/memory) and modal for full process details
    deliverables:
      - Add sort state and toggle UI to ProcessList.tsx
      - Add modal component for process details (use Dialog from @/app/components/ui/Dialog)
      - Implement modal content with full ps aux output for selected process
      - Update component unit tests
    risk: P1
    status: passing
    verify: pnpm typecheck
    estimated_hours: 3

  - title: Fix process data source
    description: Update RPC endpoint to fetch actual `ps aux` output instead of cached monitor metrics
    deliverables:
      - Update `lib/rpc/procedures/monitor.ts` to run `ps aux` on target hosts via sparkrun host exec
      - Update tests for the new data source
    risk: P0
    status: passing
    verify: pnpm typecheck && pnpm lint
    estimated_hours: 4

  - title: Performance optimization
    description: Optimize the process data fetching to avoid performance issues
    deliverables:
      - Document performance characteristics
      - Add caching or sampling if needed
      - Benchmark with realistic cluster sizes
    risk: P2
    status: passing
    verify: pnpm typecheck && pnpm lint
    estimated_hours: 2

total_estimated_hours: 14
