import { describe, expect, it } from "vitest";
import { normalizeProcessList } from "@/lib/rpc/procedures/processes";

// Simple test for normalizeProcessList (unit tests)
// ProcessList component tests would require @testing-library/react which is not in this project
